# CONTENT-TODO

Everything the team still has to supply. All of it is in `index.html` unless stated
otherwise. Search for `TODO(content)` to jump between the items.
After filling one in, delete its `<span class="tbd">TBD</span>` chip and its comment.

## Hero

- [x] **Project title** — done.
- [x] **One-sentence summary** — done.
- [x] **Member names in the wordmark** — done. Keep the letters T U R M E in order.

## Project

- [x] **The problem** — done.
- [x] **Our solution** — done.
- [x] **At a glance** — `Domain`, `Status` and `Timeline` are filled in. Update `Status`
      as the project moves from implementation to testing.

## Visuals

- [ ] **Figures** — the four existing images are in place. To add another, copy one
      `<figure>` block and drop the file into `assets/img/`.
- [ ] **Demo video** — replace the `panel--video` block with a link to the recorded demo.

## Team (one set per member, N = 1…5)

- [x] **Photo** — `assets/img/team/member-N.jpg` (square portraits, JPEG). To swap one,
      overwrite the file and keep the filename.
- [x] **Name** — real names are in place.
- [x] **Role** — all five members share the same role line.
- [ ] **Bio** — replace the two-sentence `.card__bio` text for each member, then delete
      that card's `<span class="tbd">TBD</span>` chip.
- [x] **LinkedIn** — real profile URLs are in place.

## Reports

Every link must be publicly viewable (Anyone with the link → **Viewer**). Verify each one
in a logged-out / private window — a link that only works while you are signed in will
fail for the graders.

- [x] Project Proposal — PDF + DOCX
- [x] Project Specifications Report — PDF + DOCX
- [x] Analysis Report — PDF + DOCX
- [x] PKE-Plan — PDF + DOCX
- [ ] `data-todo="design-pdf"` / `design-docx` — High-Level Design Report (not written yet).
      Set both links and remove their `link-todo` class when the file is ready.
- [x] Shared Drive folder for the **Open Drive folder** button

## Advisor

- [ ] Replace `To be announced` with the advisor's title and full name, and update the
      muted line below it.

## Contact and footer

- [x] **Team email** — `turme05@gmail.com`.
- [ ] **Footer date** — update `Last updated: 2026-08-05` whenever the site changes.
